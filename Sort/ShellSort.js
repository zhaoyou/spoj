function shellSort(arrays) {
h = 1
while h < n, h = 3*h + 1
while h > 0,
    h = h / 3
    for k = 1:h, insertion sort a[k:h:n]
    → invariant: each h-sub-array is sorted
end

var h = 0;
var n = arrays.length;
while(h < n) {
  h = 3 * h + 1;
  while ( h > 0) {
    for(var k = 1; k < h;
    h = parseInt(h / 3);
  }
}

}
